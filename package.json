{
  "name": "deep-research-agent-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend install --package-lock=false && npm --prefix frontend run build"
  }
}